# 2ブラウザ診断テストシナリオ

## 目的

2種類のブラウザで同じルームへ入り、入室・ロール・素材・配札・作品提出・披露・親引き継ぎ・次節進行のどこで状態が食い違うかを確認する。診断ログを表示するには、URL末尾に`&diag=1`を追加する。例：`haiku.html?room=ROOM_ID&diag=1`。

## 準備

ブラウザAを親用、ブラウザBをプレイヤー用とする。ブラウザAはPCまたはChrome、ブラウザBはスマートフォンまたは別ブラウザを使う。両方で同じルームIDを使用し、名前は必ず異なるものにする。例として、Aを「親テスト」、Bを「子テスト」とする。

テスト中は、画面の親表示、参加者カード、診断ログの次の項目を確認する。

| 項目 | 期待値 |
|---|---|
| `myUid` | ブラウザごとに異なる。末尾6文字だけ表示される |
| `myName` | 入力した名前と一致する |
| `isSpectator` | プレイヤーは`false`、見学者は`true` |
| `currentHost` | 現在の親名と一致する |
| `currentHostUid` | 親の`myUid`と一致する |
| `players` | プレイヤー名が重複なく入る |
| `status` | ロビーは`lobby`、開始後は`playing` |
| `schemaVersion` | 新規ルームは`2` |

## Haikuシナリオ

### A. 入室とロール確認

1. 両ブラウザで診断ログを有効にして同じルームへ入室する。
2. ブラウザAはプレイヤー、ブラウザBもプレイヤーとして入室する。
3. Aのログで`haiku-join-input`と`haiku-room-state`を確認する。
4. Bのログでも同じ項目を確認する。
5. Aだけに「親・選者」、Bには「プレイヤー」と表示されることを確認する。

### B. 素材提出と配札

1. AまたはBから素材を1件以上提出する。
2. 両方のログで`status=lobby`、`players`が2名であることを確認する。
3. Aで不足素材を補充する。
4. Aで「句会を始める！」を1回押す。
5. Aのログに`dealHaikuHands:start`、続いて`dealHaikuHands:success`が出ることを確認する。
6. 両ブラウザで`status=playing`になり、それぞれ自分の手札だけが表示されることを確認する。
7. Bで開始ボタンを押した場合は、`dealHaikuHands:error`と権限拒否が記録されることを確認する。これは期待される拒否である。

### C. 句提出と披露

1. A・Bそれぞれで手札から五音・七音・五音を選ぶ。
2. 「一句披露する！」を押す。
3. 各ブラウザで`submitHaikuPhrase:start`と`submitHaikuPhrase:success`を確認する。
4. 親が相手の句を披露する。
5. `revealHaikuPhrase:start`と`revealHaikuPhrase:success`を確認する。
6. 失敗した場合は、同じ操作について`error`の`code`、`message`、その直後の`haiku-room-state`を記録する。

### D. 親引き継ぎと次節進行

1. ラウンド中にブラウザBで「親を引き継ぐ」を押す。
2. Bのログに`claimHost:start`と`claimHost:success`が出ることを確認する。
3. Bの`haiku-room-state`で、`currentHost`がBの名前、`currentHostUid`がBの`myUid`末尾と一致することを確認する。
4. Aでは親表示がBへ変わり、「次の節に進む」が無効になることを確認する。
5. Bで「次の節に進む」を押す。
6. Bのログに`advanceHaikuRound:start`と`advanceHaikuRound:success`が出ることを確認する。

## Poemシナリオ

Haikuと同じ準備で、PoemページのURLへ入り、`poem-room-state`、`poem-join-input`を確認する。親による配札、プレイヤーの途中参戦、作品提出、作品公開、リアクション、親引き継ぎを順に操作する。異常が出た操作では、診断ログパネル下部の「ログをコピー」を押してログ全文を保存する。

## 失敗時に送る情報

失敗した場合は、次の4点を一緒に保存する。

1. 失敗したブラウザと名前（AまたはB）。
2. 失敗直前の画面スクリーンショットまたは動画。
3. 診断ログのコピー内容。
4. 失敗した操作名と、押した時刻。

UIDやルームIDは診断画面で末尾だけ表示される。パスワードや認証コードは送らない。

## 診断完了の条件

開始・句提出・披露・親引き継ぎ・次節進行について、各ブラウザの`start`と`success`が対応し、`currentHostUid`と実際の親の`myUid`が一致していることを確認できれば、診断は完了とする。診断が終了したら、URLから`diag=1`を外して通常画面へ戻す。
