{
  "name": "wa-awesome-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/participant-utils.test.mjs tests/participant-concurrency.test.mjs tests/wordset-utils.test.mjs tests/host-recovery.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore --project demo-wa-awesome \"node --test tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^14.16.0"
  }
}
