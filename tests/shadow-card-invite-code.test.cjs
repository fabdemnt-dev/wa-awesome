'use strict';
const test=require('node:test');const assert=require('node:assert/strict');const{createInviteCode,parseInviteCode,mac,safeEqual,hashIp}=require('../functions/shadow-card-online/invite-code');
test('invite code has fixed format and round trips',()=>{for(let i=0;i<100;i++){const value=createInviteCode();assert.match(value.code,/^SC2-[0-9A-HJKMNP-TV-Z]{6}-[0-9A-HJKMNP-TV-Z]{10}$/);assert.deepEqual(parseInviteCode(value.code),{locator:value.locator,secret:value.secret})}});
test('normalization accepts separators and rejects malformed codes',()=>{const v=createInviteCode();assert.deepEqual(parseInviteCode(v.code.toLowerCase().replaceAll('-',' ')),{locator:v.locator,secret:v.secret});assert.equal(parseInviteCode('SC2-TOO-SHORT'),null)});
test('HMAC comparison and rotating IP hash are deterministic',()=>{const a=mac('ABC123','SECRET0000','test-key');assert.equal(safeEqual(a,mac('ABC123','SECRET0000','test-key')),true);assert.equal(safeEqual(a,mac('ABC123','SECRET0001','test-key')),false);assert.equal(hashIp('192.0.2.1','key','2026-09-06'),hashIp('192.0.2.1','key','2026-09-06'));assert.notEqual(hashIp('192.0.2.1','key','2026-09-06'),hashIp('192.0.2.1','key','2026-09-07'))});
